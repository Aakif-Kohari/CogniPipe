/**
 * @module @cognipipe/sdk
 *
 * Public API for the CogniPipe SDK.
 * Provides the BaseNode abstract class and @CogniNode() decorator
 * that every community-contributed node must extend and use.
 *
 * @example
 * ```typescript
 * import { BaseNode } from '@cognipipe/sdk';
 * import { CogniNode } from '@cognipipe/sdk';
 * ```
 */

// Exports will be added here as BaseNode and @CogniNode() are implemented.
// This file must remain a re-export barrel — no logic lives here.
